生产者编程步骤：
1、创建生产者，注册到某个组（与消费者保持同一组）
2、设置nameserv地址
3、调用start()方法，开启生产者
4、发送消息
  1)消息设置setDelayTimeLevel延迟级别，发送延迟消息
  2)send()方法发送消息，不设置回调函数为同步，设置为异步，发送到broker后，根据成功失败结果，同步消息返回状态码，异步消息调用回调函数
  3)sendoneway()方法发送单向消息
5、调用shutdown()，关闭生产者